// ARRAYS
// This is a list of items

// declare an array

let users = ["Jane", "John", "Jack" ,"Jared", "Otieno"];
console.log(users);
// accessing an item in the array  achieved using the square bracket notation
let sampleUser = users[0];
console.log(sampleUser);
// get (coount the number of items in the array)
let numOfUsers - users.length;
console.log(numOfUsers);
// get an element from the end of an array
let lastItem = users[users.length -1]; 
console.log(lastItem);

// add an item to the array
// at the end
users.push("Jayden");
console.log(users);